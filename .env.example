# Hash SHA-256 de la clé API (générer avec : hashlib.sha256(key.encode()).hexdigest())
API_KEY=<hash_sha256_de_votre_cle>

# Connexion à la base de données PostgreSQL
DB_HOST=<your_db_host>
DB_PORT=5432
DB_USER=your_user
DB_PASSWORD=your_password
DB_NAME=attrition_db